# 项目依赖（pytest 测试 + GPS注入脚本都要用）
appium-python-client>=5.0   # Appium客户端 (UI自动化)
gpxpy>=1.6                  # GPX解析 (GPS注入)
pytest>=8.0                 # 测试框架
pytest-cov>=5.0             # 覆盖率报告
pytest-html>=4.0            # HTML测试报告
pytest-timeout>=2.0         # 用例超时保护 (UI用例防卡死)
